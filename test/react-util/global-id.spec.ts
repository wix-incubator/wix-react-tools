import * as React from 'react';
import { expect } from 'test-drive-react';

describe('GlobalID', () => {
   describe('getRootId', () => {

   });

   describe('getLocalId', () => {

   });

   describe('Helper methods', () => {

       class TestClass extends React.Component {}

       describe('isClassInstance', () => {
            it('detects that an object is an instance of a component class', () => {
                expect(true).to.be.ok;
            });

            it('detects that an object is not an instance of a component class', () => {

            });
       })

   });
});
